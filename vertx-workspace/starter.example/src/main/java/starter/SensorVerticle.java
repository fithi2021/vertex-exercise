package starter;

import java.util.Random;
import java.util.UUID;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import io.vertx.core.AbstractVerticle;
import io.vertx.core.Promise;
import io.vertx.core.json.JsonObject;
import io.vertx.ext.web.RoutingContext;

public class SensorVerticle extends AbstractVerticle {
	/*
	 * private static final Logger logger =
	 * LoggerFactory.getLogger(SensorVerticle.class);
	 * 
	 * private final Random random = new Random(); private final String sensorId =
	 * UUID.randomUUID().toString(); private double temperature = 21.0;
	 * 
	 * @Override public void start(Promise<Void> startPromise) {
	 * 
	 * vertx.setPeriodic(2000, this::updateTempreture); startPromise.complete();
	 * 
	 * }
	 * 
	 * private void updateTempreture(Long id) { temperature = temperature +
	 * (random.nextGaussian() / 2.0d); logger.info("Temperature updated {} ",
	 * temperature);
	 * 
	 * // System.out.println("Temperature updated {} "+temperature);
	 * 
	 * }
	 */
	
	
}
